import React, { useState } from "react";
import { View, StyleSheet } from "react-native";

const _styles = StyleSheet.create({

});

const Routes = ({toolBar, Routes}) => {
    return (
        <View>{toolBar}</View>
    );
};

export default Routes;